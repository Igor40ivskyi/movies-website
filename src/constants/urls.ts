const baseMyBackendURL = 'http://localhost:5100';

const endpoints = {
    register: '/auth/register',
    login: '/auth/login',
    users: '/users',
};

export {
    baseMyBackendURL,
    endpoints,
};
