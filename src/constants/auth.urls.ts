const baseMyBackendURL = 'http://localhost:5100';

const authEndpoints = {
    register: '/auth/register',
    login: '/auth/login',
    users: '/users',
};

export {
    baseMyBackendURL,
    authEndpoints,
};
